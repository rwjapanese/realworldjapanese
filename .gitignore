# build output
dist/

# build serialization lock (scripts/build-lock.sh) — runtime-only, never commit
.astro-build.lock/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# SEO reporting (scripts/seo_report.py) — never commit creds or the venv
scripts/.venv/
scripts/*.json
scripts/oauth_client.json
scripts/oauth_token.json
*-service-account*.json
gsc-ga4-key*.json

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# pagefind

public/pagefind